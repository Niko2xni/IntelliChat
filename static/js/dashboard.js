// Shared dashboard bootstrap file.
// Keep this file present because dashboard/base.html includes it on every dashboard page.

document.addEventListener('DOMContentLoaded', () => {
    // Intentionally lightweight placeholder for shared dashboard behavior.
});
